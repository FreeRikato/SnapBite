export type Gender = "male" | "female";
export type Goal = "lean" | "maintain" | "gain";

export type BmiCategory = {
	label: string;
	color: string;
};

export type CalorieModeInfo = {
	label: string;
	description: string;
};
